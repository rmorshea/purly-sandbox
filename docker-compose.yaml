version: '3'
services:
  nginx:
    restart: always
    build: ./nginx/
    ports:
      - 80:80
    depends_on:
      - editor
  editor:
    restart: always
    build: ./editor
    depends_on:
      - state
    read_only: true
  state:
    restart: always
    build: ./state
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
  redis:
    restart: always
    image: redis
